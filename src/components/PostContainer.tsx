import React from 'react';
import {postAPI} from "../services/PostsService";

const PostContainer = () => {
    const {} = postAPI.
    return (
        <div>
            
        </div>
    );
};

export default PostContainer;